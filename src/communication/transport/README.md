# Transport boundary

Future byte-stream transports belong here. They may expose open/close,
connected/disconnected, byte reception, byte writes, and transport errors.
They must not know motors, firmware, widgets, or page IDs. No transport is
implemented in the current preview-only phase.
