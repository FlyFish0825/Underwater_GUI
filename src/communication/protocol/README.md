# Protocol boundary

Future incremental frame decoders and protocol-family encoders belong here.
They may convert bytes to protocol objects and protocol objects to bytes, but
must not include QWidget or display text. No AA55/AA56 parser is implemented
until a future task explicitly authorizes real communication.
